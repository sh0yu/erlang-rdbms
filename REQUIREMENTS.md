# 要件と、Postgres とのフィット&ギャップ

## 1. 設計思想

### 前提の置き換え

既存の RDBMS は、**クライアントとデータベースが一つの故障単位である**かのように
設計されている。接続が生きている間だけが世界で、切れたら忘れる。
PostgreSQL の backend も MySQL のスレッドも、接続が消えた瞬間に消える。

しかし現実には両者は別の故障単位で、その間にはネットワークがある。
だから「要求は届いたのか」がクライアントに分からない瞬間が必ず存在する。

既存のデータベースはこの瞬間の後始末をアプリケーションに押し付けてきた。
Cassandra は WriteTimeoutException を投げ、FoundationDB は
commit_unknown_result を文書化し、Spanner も同じで、
どれも「冪等に設計しろ」と言う。

tether はこの境界を設計の中心に置く。

### 三つの原則

**(1) セッションは接続ではなく、クライアントである**

同一性の単位を接続からクライアントへ移す。セッションは切断・プロセス死・
ノード障害を越えて生き、記憶を保つ。

**(2) データベースは「何をしたか」ではなく「何と答えたか」を覚える**

復旧のためだけなら操作を記録すれば足りる。しかしそれでは再送に対して
初回と同じ答えを返せない。**答えを記録することが exactly-once の条件**である。

**(3) 隔離はプロセス単位である**

多テナントの共有サービスとして、あるクライアントの都合が他人の応答時間に
出てはいけない。ヒープ、CPU、故障の全てをクライアント単位で分ける。

### 意図的に捨てるもの

* **関係モデル。** SQL も JOIN も集約も持たない。値はバイト列で、
  意味はアプリケーションが持つ
* **メモリを超えるデータ。** 全データがメモリにある前提で設計する
* **汎用性。** 分析にも報告にも向かない

捨てる理由は、上の三原則と両立しないからではなく、
**そこは Postgres の方が良いから**である。

## 2. 想定する適用範囲

向く:

* データがメモリに載る (〜100GB 程度)
* クライアントが多い (10^5〜10^6)
* クライアントが不安定な回線の先にいる (携帯、辺縁、組込み)
* 再送の安全性が効く領域 (元帳、注文、在庫、課金)

向かない:

* 大規模データ、分析、報告
* 複雑な問い合わせ、随時の集計
* スキーマで整合性を守りたい業務

## 3. 機能要件 — フィット&ギャップ

凡例: ✓ 有 / ✗ 無 / △ 部分的
状態: [済] 実装済 / [部] 一部 / [未] 未着手 / [外] 対象外

### 3.1 Postgres にあり、tether も持つ (土台)

| # | 要件 | PG | tether | 状態 |
|---|---|---|---|---|
| F1 | 鍵による読み書き | ✓ | ✓ | [済] |
| F2 | 複数鍵の原子的更新 | ✓ | ✓ | [済] |
| F3 | 条件付き更新 (CAS) | ✓ | ✓ | [済] |
| F4 | 永続性 (fsync してから応答) | ✓ | ✓ | [済] |
| F5 | クラッシュ復旧 | ✓ | ✓ | [済] |
| F6 | チェックポイントとログ切り詰め | ✓ | ✓ | [済] |
| F7 | 破損の検出 | △ 既定で checksum 無効 | ✓ CRC + 起動拒否 | [済] |
| F8 | 範囲走査・順序付き読み取り | ✓ | 必要 | [未] |
| F9 | 二次索引 | ✓ | 必要 | [未] |
| F10 | バックアップ・PITR | ✓ | 必要 | [未] |
| F11 | レプリケーション | ✓ | 必要 | [未] |

### 3.2 Postgres にあり、tether は持たない (意図的)

| # | 要件 | 理由 |
|---|---|---|
| F12 | SQL・関係代数・JOIN・集約 | Postgres の方が良い。競う意味がない |
| F13 | スキーマ・型・制約 (PK/FK/UNIQUE/CHECK) | 値はバイト列。意味はアプリが持つ |
| F14 | ビュー・トリガ・ストアドプロシージャ | 同上 |
| F15 | 全文検索・地理空間・拡張機能 | 同上 |
| F16 | MVCC による読み書きの非阻害 | 状態が不変値なので、別の解き方をする |

### 3.3 Postgres に無く、tether が持つ (存在理由)

| # | 要件 | PG | 状態 |
|---|---|---|---|
| **F17** | **再送の吸収 (exactly-once)** | ✗ 冪等キーの表を自作させる | **[済]** |
| **F18** | **接続を越えて生きるセッション** | ✗ backend は接続と同時に消える | **[済]** |
| **F19** | **答えの記録と、再送への再返答** | ✗ | **[済]** |
| **F20** | **クライアント単位の資源上限** | ✗ | **[済]** |
| F21 | オフライン中の操作の受け入れと統合 | ✗ | [未] |
| F22 | 復帰時の差分押し出し | △ LISTEN/NOTIFY・論理レプリケーション | [未] |
| F23 | セッションの失効と回収 | 接続が切れれば消える | [部] |

## 4. 非機能要件 — フィット&ギャップ

| # | 要件 | Postgres | tether | 状態 |
|---|---|---|---|---|
| N1 | 同時セッション数 | 数百。PgBouncer が事実上必須 | **100万** | [済] |
| N2 | セッションあたりの資源 | 数MB (backend プロセス) | **約3.3KB** (実測) | [済] |
| N3 | セッション生成 | fork。ミリ秒の桁 | **20〜28µs** (実測) | [済] |
| N4 | テナント間の遅延の独立 (p99) | ✗ 共有バッファ・autovacuum・checkpoint が全員に出る | ヒープ隔離・先取り | [部] |
| N5 | 1クライアントの暴走の封じ込め | ✗ backend の異常終了は**全接続の再起動**を招く | プロセス単位で死ぬ | [済] |
| N6 | コミットあたりの fsync | 1回 (group commit あり) | 1回 (500件/fsync 実測) | [済] |
| N7 | 書き込みスループット | 数万 tx/s | 未測定。**ストアが単一の直列点** | [未] |
| N8 | 読み取りの並列度 | 高い | ✗ ストアを経由して直列 | [未] |
| N9 | **扱えるデータ量** | **メモリを超えてよい** | **メモリに載る分だけ** | **構造的制約** |
| N10 | **起動時間 (RTO)** | WAL 再生。データ量に依存しない | **全データをメモリへ読み直す** | **構造的制約** |
| N11 | 運用性 (監視・移行・道具) | 成熟 | ほぼ無い | [未] |

N9 と N10 は実装で埋まる種類のものではない。**設計の帰結**であり、
適用範囲を決める境界そのものである。

## 5. Postgres が実は持っている近いもの (正直な検討)

「Postgres にできない」と言う前に、近いものを検討しておく。

### 5.1 二相コミット (PREPARE TRANSACTION)

`pg_prepared_xacts` を見れば、宙に浮いたトランザクションが
コミットされたかどうかを**後から判定できる**。つまり
コミット不明の解決手段は存在する。

しかし:

* `max_prepared_transactions` は**既定で 0**、つまり無効
* 準備済みトランザクションは**ロックを無期限に保持する**。
  クライアントが消えると、そのロックは人が始末するまで残る。
  これが既定で無効な理由であり、多くの現場で禁止されている理由
* 識別子 (gid) の採番と追跡はアプリケーションの仕事
* 分かるのは「コミットされたか」だけで、**初回の答えは返らない**
* 最低2往復

### 5.2 レプリケーションスロット

論理レプリケーションスロットは、まさに**接続を越えて生きる
クライアント状態**である。切断しても消えず、どこまで読んだかを覚えている。

つまり Postgres はこの形を持っている。ただし**レプリケーション専用**で、
普通のクライアントには開かれていない。そして消えたクライアントのスロットが
WAL を溜め続けてディスクを埋める事故は有名で、この形が
「安いとは限らない」ことも示している。

### 5.3 冪等キーの表

```sql
CREATE TABLE idempotency (client text, seq bigint, response jsonb,
                          PRIMARY KEY (client, seq));
```

**動く。** これが実務の標準解である。だから「不可能」ではない。

しかし:

* 全ての書き込み経路で守る必要があり、1箇所忘れれば穴が空く
* 応答も保存しなければ意味がないが、多くの実装は「重複です」としか返さない
* 行は何もできない。統合も押し出しも自己失効も、別のワーカーが要る
* いつ消してよいか誰も知らない
* **アプリケーションの規律であって、データベースの機能ではない**

### 結論

tether の主張は「Postgres には不可能」ではなく、

> **クライアント境界の始末を、アプリケーションの規律ではなく
> データベースの原始機能にする。そしてそのために必要な
> 「生きていて、かつ安いセッション」を持てるのは BEAM だけである。**

## 6. いまどこまで満たしているか

```
存在理由 (3.3)          F17 exactly-once            [済]
                        F18 接続を越えるセッション   [済]
                        F19 答えの記録と再返答       [済]
                        F20 資源上限                 [済]
                        F21 オフライン中の操作       [未]  ← 次
                        F22 復帰時の差分押し出し     [未]  ← 次
                        F23 失効と回収               [部]

土台 (3.1)              F1〜F7                       [済]
                        F8 走査 / F9 索引            [未]
                        F10 バックアップ / F11 複製  [未]

非機能                  N1〜N3 セッションの安さ      [済] 実測あり
                        N5 暴走の封じ込め            [済]
                        N6 fsync                     [済]
                        N4 p99 の独立                [部] ストアが直列点
                        N7 N8 スループット           [未] 未測定・未対処
                        N9 N10                       構造的制約として受け入れる
                        N11 運用性                   [未]
```

**存在理由のうち、「切断しても答えが分かる」までは満たした。
「切断中も動ける」には届いていない。**

F21 と F22 が入って初めて、セッションが生きていることの本来の
使い道が出る。いまはまだ「死なない記憶」でしかない。

次に重い順:

1. **F21・F22 (オフライン統合と差分押し出し)** — 存在理由の残り半分
2. **N7・N8 (ストアの直列点)** — 鍵空間で分割すれば並列になる
3. **F8・F9 (走査と索引)** — 鍵の読み書きだけでは実用に足りない
4. **F11 (レプリケーション)** — 1ノードでは可用性が無い
