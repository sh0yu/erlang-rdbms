# tether — セッションが接続より長生きするデータベース

## 何を解くのか

分散データベースの実装を調べた結果、**汎用データベースで exactly-once を
実現しているものが存在しない**ことが分かった。

  * Cassandra   — `WriteTimeoutException` は「必要な数のACKが返らなかった」
                  という意味であって、書き込みが失敗したという意味ではない。
                  部分的に書かれたものは巻き戻らず、hinted handoff と
                  read repair がそれを積極的に伝播させる。再送すると
                  カウンタは二重に数える
  * FoundationDB — `commit_unknown_result` を明示的に文書化し、
                  冪等な設計をアプリケーションに要求している
  * Spanner     — 同様。トランザクションは原子的だが、
                  クライアントから見たコミットの不確実性は残る
  * TigerBeetle — **解いている。** クライアントのセッションと通番を
                  レプリケートされた状態機械の中で重複排除する。
                  ただし複式簿記専用で、汎用ではない

つまり、どのデータベースも「届いたかどうか分からない」をアプリケーションに
押し付けている。冪等キーの表を自分で作れ、と。

## なぜ既存の実装ができないのか

再送を吸収するには、サーバ側がクライアントごとに「どの要求を実行したか」と
「そのとき何を返したか」を覚えている必要がある。

既存のアーキテクチャでは、その置き場所が2つしかない。

  行にする    動く。冪等キーの表がそれ。しかし行は何もできない。
              オフライン中に溜まった操作の統合も、復帰時の差分押し出しも、
              自己失効もできない。別のワーカーが要る。そして何より
              **アプリケーションの規律であって、データベースの機能ではない**

  スレッドにする  セッションが振る舞いを持てる。しかしOSスレッドは
              1本あたり数十KB〜数MB。100万セッションは載らない

だから既存のデータベースは「接続 = セッション」にして、切れたら忘れる。
PostgreSQL の backend も MySQL のスレッドも、接続が切れた瞬間に消える。

## なぜ BEAM ならできるのか

BEAM のプロセスは**生きていて、かつ安い**。

  生成      数マイクロ秒
  最小      約2.5KB           → 100万セッションで約2.5GB。1台に載る
  ヒープ     プロセスごとに独立  → GCが他を止めない。p99が汚染されない
  上限      max_heap_size     → 食い潰しても、そのプロセスだけ死ぬ
  先取り     約4000リダクション → 暴走しても他は止まらない
  監視      VMの原始機能       → 死ねば復帰。ノードをまたいでも検知できる

軽量な並行実体を大量に持てるだけなら Go の goroutine や .NET Orleans も
できる。BEAM が唯一なのは、**それに加えてヒープが隔離されていて、
先取りされ、監視が原始機能である**点。データベースは多テナントの共有
サービスなので、あるクライアントの都合が他のクライアントの応答時間に
出てはいけない。共有ヒープでは、1人のGCが全員のp99を悪化させる。

## 設計の中心

  セッションは接続ではない。**クライアントごとに1つの、永続的で、
  監視された、移動可能なプロセス**である。

  切断しても、プロセスが死んでも、ノードが落ちても生き残る。
  実行済みの要求を覚えていて、再送を必ず吸収する。

保証:

  1. **exactly-once** 同じ (client, seq) は高々1回しか実行されない。
     再送には、初回とまったく同じ応答を返す
  2. **原子性** 1つの要求に含まれる複数の操作は、全部適用されるか
     まったく適用されないか
  3. **永続性** 応答を返したものは、電源断を越えて残る

保証しないこと(現時点):

  * 複数ノードへのレプリケーション。まず1ノードで正しくする
  * 問い合わせ言語。まずキーによる読み書きだけ

## 段取り

  0  骨格            OTPアプリ、supervisor、公開API の形                 [済]
  1  ログの表現      レコード形式、CRC、切り捨てと破損の検出（純粋）      [済]
  2  ログの書き込み   ファイル所有、追記、fsync、group commit              [済]
  3  ストア          データの適用。原子的な複数キー操作                    [済]
  5  復旧            ログ再実行でデータとセッションを再構築                [済]
  4  セッション      プロセス、重複排除、応答の保存                        [済]
  6  隔離の実証      食い潰し・異常終了が他に影響しないことを試験で示す    [済]
  7  切断と再接続    TTL、失効、ログの切り詰め                             [済]
  8  クライアント     exactly-once が効いていることを見せるデモ            [済]
  9  時刻を入力に    ctx で渡す。復旧の再実行で答えが変わらないように      [済]
 10  預かり(escrow)  数値の下限を協調なしで守る。local-first の中核        [済]
 11  束の流し込み    溜めた操作を順に流し、最初の失敗で止める              [済]
 12  resume          自分の通番と預かりを聞き直す                          [済]
 13  計測            協調が何分の1になるか、在庫の遊休はどれだけか        [ ]
 14  差分の押し出し  復帰時に見落とした変更を送る                          [ ]

段階ごとに EUnit と dialyzer を通す。

## 実測

  group commit    500件の書き込みが fsync 1回
  ストア          300件の submit が fsync 3回（ストアは fsync を待たない）
  セッション      約3.3KB/個、生成 20〜28µs/個
                  → 100万セッションで約3.1GB。1台に載る

## 作りながら分かったこと

### max_heap_size はプロセスのメモリ上限ではない

隔離の主張を試験にしたところ、最初の版が落ちた。**64バイトを超える
バイナリはプロセスヒープに載らない。** 参照計数される共有領域に
置かれ、プロセスヒープにはヘッダだけが残る。13MB のバイナリを
含む要求を投げたセッションのヒープは 34KB のままで、上限に一度も
触れなかった。

したがって隔離は max_heap_size 任せにできない。要求の大きさを
明示的に見て、**セッションへ渡す前に**拒む必要がある
(tether_data:validate/1)。max_heap_size は二段目の防壁として、
ヒープ上で膨らむ場合にだけ効く。

本番では通信路の復号器が同じ検査をすることになる。境界で弾かなければ、
弾く頃にはもう確保が済んでいる。

## 預かり(escrow) — local-first の中核

「在庫 ≥ 0」のような数値の下限は、協調なしには守れない。しかし
**協調が要るのは在庫が尽きるときだけ**である。潤沢なうちは中央から
持ち分を配ってしまえば、保持者は問い合わせずに消費できる。配った
総和が在庫を超えないので、下限は自動的に守られる。

    在庫 1000 → alice に 5、bob に 5、中央に 990
    alice は圏外でも5個売れる。協調ゼロ。合計は絶対に 1000 を超えない

これが既存の同期エンジン(ElectricSQL, PowerSync, Replicache)や CRDT との
分かれ目である。どれもオフラインの書き込みは受け付けるが、**共有された
有限資源の下限は守れない**。CRDTカウンタで「10から1引く」と「10から7引く」を
統合すれば 2 になり、在庫が3しか無くても8個売れる。預かりなら bob は
5 しか持っていないので 7 はそもそも売れない。

配る量は tcmalloc のスレッド局所キャッシュ、TCP の輻輳窓と同型に
自動で決める。実測した退化の推移(在庫1000、保持者が順に現れる場合):

    [500, 250, 62, 31, 19, 13, 10, 8, 6, 5, 4, 4]

崖が無く、最後は 1 に落ちて古典のロックと同じ振る舞いになる。
そして**壊れる設定が存在しない**。配りすぎれば在庫が遊ぶだけ、
配らなすぎれば協調が増えるだけで、下限は配った総和の上限で
守られているので設定値に依存しない。

つまみは2つで、どちらも製品側の言葉になる。

    リース期間      = 何時間のオフラインを支えるか
                      長い = オフライン耐性が高い = 在庫が遊ぶ
    期限切れの扱い  = 既定は厳格(期限後の売上は無効)

## 束の流し込みは、最初の失敗で止める

オフライン中に積んだ操作には順序の意味がある。前提が崩れた後の操作を
流すと、成立しない結果を作る。

    seq n   在庫を1個引き当て → 前提が変わっていて失敗
    seq n+1 注文を確定        → **流してはいけない**

だから最初の失敗で止め、どこまで通ったかを返す。そこから先の判断は
クライアント(あるいは人)に返す。

通番は束全体で1つにした。各要素に別々の通番を振ると、覚えている答えが
1件では足りなくなる。束ごと1件なら、途中で接続が切れても同じ束を
そのまま送り直せば吸収される。ログも1レコード、fsync も1回。

## 既知の限界

  * **ストアが単一の直列点。** 全クライアントの書き込みが1プロセスを
    通る。セッションは並列だが、適用は直列。鍵空間で分割すれば
    並列になるが、まだやっていない
  * 読み取りもストアを経由する。BEAM では大きな不変構造を
    プロセス間で共有できない(コピーになる)ので、スナップショット読み取りは
    ただでは手に入らない
  * 未応答の要求はクライアントあたり1件まで。覚えている答えも直前の1件だけ
  * ログが伸び続ける。切り詰め(スナップショット)がまだ無い
  * 1ノードのみ。レプリケーションは無い
  * 問い合わせは鍵による読み書きのみ。走査も索引も無い
  * **預かりの回収が O(保持者数)。** 中央が足りなくなったとき、その資源の
    保持者を順に見て期限切れを探す。保持者が非常に多い資源では
    期限順の索引が要る
  * **最初の保持者への配分が大きすぎる。** 中央の残りを保持者数で割るので、
    保持者が1人のときは半分を持っていく。あとから来る保持者を
    見越した上限が無い。Step 13 の計測で調整する
  * **認証・認可が無い。** resume も session_info も、誰でも他人のものを
    読める。この層の外で解く前提だが、まだ何も無い
  * 復帰時の差分押し出し(F22)が無い。セッションが変更を購読する能力が要る
