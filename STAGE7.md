# Stage 7 — プランナと最適化

計画(`rdbms-erlang-humming-meadow.md`)の Stage 5 にあたる、最後の未実装段階。
Stage 1〜6 で「SQLが動く」ところまでは通っている。ここで足すのは
**どう動かすかを選ぶ層**である。

現状の問題:

* `SELECT` は常に全表走査する。索引があっても使わない
* 述語は結合の**上**で評価される。結合してから捨てるので、無駄に結合する
* 結合順序は FROM に書いた順のまま
* 全カラムに自動で索引が張られるので、「索引があるか」が常に真になり、
  索引選択が退化する
* 実行計画を見る手段が無いので、最適化が効いているかを検証できない

## 段取り

| | 内容 | 検証 |
|---|---|---|
| **7-1** ✅ | 論理プランの導入(AST → 論理 → 物理 の2段化)と `EXPLAIN` | EXPLAIN の出力に対するテスト |
| **7-2** ✅ | 述語のプッシュダウン | 「この述語が結合の下に落ちている」を構造として検査 |
| **7-3** ✅ | `CREATE INDEX` / `DROP INDEX`。自動全カラム索引をやめる | 索引の有無で挙動が変わること |
| **7-4** ✅ | 統計。行数の維持と `ANALYZE` による異なり値数の採取 | 統計値そのもの |
| **7-5** ✅ | 索引スキャン(アクセスパス選択)。等値と範囲 | EXPLAIN が index_scan を選ぶこと |
| **7-6** ✅ | 結合順序。3表以上で推定カーディナリティによる貪欲法 | 順序が入れ替わること |

各段でEUnitとdialyzerを通し、既存301件を壊さない。

**Stage 7 完了。**

その後に足したもの:

| | 内容 | 実測 |
|---|---|---|
| 並行制御 段階1 | `BEGIN READ ONLY`。読み取り専用は直列化の列に並ばない | 8接続の読み取りで 94.4ms → 14.1ms |
| 並行制御 段階2 | スナップショット分離。`snapshot_mng` が undo を持ち、読み手を開始時点へ巻き戻す。索引は候補を広げて絞り直す | 6.2秒かかる照会が走っている間に50コミットが161msで完走(従来は照会の終わりまで待っていた) |
| 並行制御 段階3 | 書き手の並行化。行ロック + 衝突検査 + デッドロック検出 + 表の共有ロック | 8接続の書き込みで 218 → 362 tx/s(fsyncなしで 559 → 1762 tx/s) |
| `READ COMMITTED` | 文ごとにスナップショットを取り直し、ロック待ちの後は読み直して文をやり直す | 100接続が同じ行を +1 する例で、やり直し 4,950 → 0 回 |
| 読み経路の単一プロセス除去 | カタログはETSキャッシュから直読み、走査は呼び出し元がバッファのETSを直読み | 32接続の同時走査で 727 → 6957 scans/s |
| ハッシュ結合 | 等値で結べるときに使う | 500×500 の結合で 44.5ms → 1.6ms |
| 集合演算 | `UNION` / `INTERSECT` / `EXCEPT` [`ALL`] | — |
| 導出表・列別名 | `FROM (SELECT ...) AS t`、`expr AS name` | — |
| 副問い合わせ | `IN` / `EXISTS` / スカラー(相関しないもの) | — |
| スカラー関数 | `UPPER` `COALESCE` `ROUND` 等、`CASE`、`LIKE` | — |

476 tests, dialyzer clean.

## 途中で見つけた既存のバグ

作りながら露出したもの。どれも既存の試験を通り抜けていた。

| 症状 | 原因 |
|---|---|
| 索引の無いカラムへの検索が**黙って空**を返す | `simple_index` が索引の無いカラムに `[]` を返す。全カラムに自動で索引が張られていたのでこの経路に入っていなかった |
| `SELECT COUNT(*) AS n FROM t` が**接続プロセスを落とす** | 集約の検出が別名(`#aliased{}`)を見ていない。`GROUP BY` があると別経路を通るのですり抜けていた |
| `t RIGHT JOIN u` が**黙って内部結合になる** | `right` が予約語でないので「t に right という別名」と解釈される |
| `sql_shell` の `data_dir` が効かない | `application:load/1` が `.app` の env で `set_env` を上書きする。VM内で最初に起動したときだけ出る |
| `EXCEPT ALL` の重複度が誤り | 左の行が右の行を打ち消す処理が入っていなかった |
| タプルAPIの `UPDATE`/`DELETE` が**スナップショットを見ずに共有データを直接読む** | 直列実行の頃は安全だった。並行にすると、他の接続の適用の途中(削除と挿入の隙間)を見て0件になる |
| **全接続がカタログとバッファプールの1プロセスの前に並ぶ** | 読みも `gen_server:call` だった。32接続の同時走査でメッセージキューが31まで伸びる(接続数と一致 = 全員が待っている)。直列実行だった頃は表に出なかった |
| 読み取り専用トランザクションが**行ロックを取ったまま返さない** | ロックの解放は `tx_mng` の終了処理にあるが、読み取り専用はそこを通らない。取ったロックの持ち主は全接続で同じ `readonly` になる。同じ行を書こうとした接続が永久に待つ |

## 残っているもの

* **SSI** — スナップショット分離のままでは write skew を防げない
* **グループコミット** — コミットごとの fsync が書き込みの上限
* **DDLのロールバック** — カタログのUNDOが無い
* **範囲での索引スキャン** — 未コミット変更の重ね合わせが等値専用
* `RIGHT`/`FULL OUTER JOIN`、ウィンドウ関数、`BETWEEN`、`CAST`

## 設計方針

**論理プランと物理プランを分ける理由。** 現状 `sql_analyzer` は AST から
物理演算子(`#p_seq_scan{}` など)を直接組む。最適化を入れるには
「何をするか」と「どうやるか」を分ける必要がある。
述語プッシュダウンは論理プラン上の書き換えであり、索引スキャンの選択は
論理から物理への変換時の判断になる。

**EXPLAIN を先に作る理由。** プランナのテストは実行結果では書けない。
「述語が結合の下に落ちた」は結果を変えないからである。
構造を検査する手段が無いまま最適化を入れると、効いているかどうかが
分からないまま進むことになる。
